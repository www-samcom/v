import type { Template, GeneratedFiles } from '@/types';

export const generateTemplateFiles = (template: Template, projectName?: string): GeneratedFiles => {
  const name = projectName || template.name;
  const { palette } = template;
  const files: GeneratedFiles = {};

  files['package.json'] = JSON.stringify({
    name: name.toLowerCase().replace(/[^a-z0-9]+/g, '-'),
    version: '1.0.0', private: true,
    scripts: { dev: 'next dev', build: 'next build', start: 'next start', lint: 'next lint' },
    dependencies: {
      next: '14.2.5', react: '18.3.1', 'react-dom': '18.3.1',
      tailwindcss: '3.4.3', '@headlessui/react': '1.7.18',
      'framer-motion': '11.2.0', 'lucide-react': '0.378.0',
      clsx: '2.1.1', 'tailwind-merge': '2.3.0', '@next/font': '14.2.5',
      ...(template.tags.includes('Three.js') ? { three: '0.165.0', '@react-three/fiber': '8.16.8', '@react-three/drei': '9.105.6' } : {}),
      ...(template.tags.includes('GSAP') ? { gsap: '3.12.5' } : {}),
      ...(template.tags.includes('Charts') ? { recharts: '2.12.7' } : {}),
    },
    devDependencies: {
      typescript: '5.4.5', '@types/react': '18.3.3', '@types/node': '20.14.2',
      autoprefixer: '10.4.19', postcss: '8.4.38', eslint: '8.57.0', 'eslint-config-next': '14.2.5',
      ...(template.tags.includes('Three.js') ? { '@types/three': '0.165.0' } : {}),
    },
  }, null, 2);

  files['tsconfig.json'] = `{\n  "compilerOptions": {\n    "target": "ES2017",\n    "lib": ["dom", "dom.iterable", "esnext"],\n    "allowJs": true,\n    "skipLibCheck": true,\n    "strict": true,\n    "noEmit": true,\n    "esModuleInterop": true,\n    "module": "esnext",\n    "moduleResolution": "bundler",\n    "resolveJsonModule": true,\n    "isolatedModules": true,\n    "jsx": "preserve",\n    "incremental": true,\n    "plugins": [{ "name": "next" }],\n    "paths": { "@/*": ["./src/*"] }\n  },\n  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],\n  "exclude": ["node_modules"]\n}`;

  files['next.config.mjs'] = `/** @type {import('next').NextConfig} */\nconst nextConfig = {\n  reactStrictMode: true,\n  images: {\n    remotePatterns: [\n      { protocol: 'https', hostname: 'images.unsplash.com' },\n      { protocol: 'https', hostname: 'picsum.photos' },\n    ],\n  },\n};\nexport default nextConfig;`;

  files['tailwind.config.ts'] = `import type { Config } from "tailwindcss";\nconst config: Config = {\n  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],\n  theme: {\n    extend: {\n      colors: {\n        bg: { DEFAULT: "${palette.bg}", secondary: "${palette.bg}dd" },\n        accent: { DEFAULT: "${palette.accent}", secondary: "${palette.accent2}", glow: "${palette.accent}33" },\n        "brand-text": "${palette.text}",\n      },\n      fontFamily: {\n        sans: ["var(--font-inter)", "system-ui", "sans-serif"],\n        display: ["var(--font-space)", "system-ui", "sans-serif"],\n        mono: ["var(--font-mono)", "monospace"],\n      },\n      animation: {\n        "fade-up": "fadeUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) both",\n        "fade-in": "fadeIn 0.5s ease both",\n        float: "float 6s ease-in-out infinite",\n        "glow-pulse": "glowPulse 3s ease-in-out infinite",\n        "slide-up": "slideUp 0.5s cubic-bezier(0.16, 1, 0.3, 1) both",\n      },\n      keyframes: {\n        fadeUp: { from: { opacity: "0", transform: "translateY(24px)" }, to: { opacity: "1", transform: "translateY(0)" } },\n        fadeIn: { from: { opacity: "0" }, to: { opacity: "1" } },\n        float: { "0%, 100%": { transform: "translateY(0px)" }, "50%": { transform: "translateY(-12px)" } },\n        glowPulse: { "0%, 100%": { boxShadow: "0 0 20px ${palette.accent}40" }, "50%": { boxShadow: "0 0 60px ${palette.accent}80, 0 0 120px ${palette.accent}30" } },\n        slideUp: { from: { opacity: "0", transform: "translateY(16px)" }, to: { opacity: "1", transform: "translateY(0)" } },\n      },\n    },\n  },\n  plugins: [],\n};\nexport default config;`;

  files['.env.local'] = `# ${name} — Environment Configuration\nNEXT_PUBLIC_APP_NAME="${name}"\nNEXT_PUBLIC_APP_URL=http://localhost:3000\nNEXT_PUBLIC_ACCENT_COLOR="${palette.accent}"\nNEXT_PUBLIC_TEMPLATE_ID="${template.id}"\nNEXT_PUBLIC_GA_ID=G-XXXXXXXXXX\nNEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxx`;

  files['src/styles/globals.css'] = `@tailwind base;\n@tailwind components;\n@tailwind utilities;\n@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=Space+Grotesk:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap');\n:root { --accent: ${palette.accent}; --accent-2: ${palette.accent2}; --bg: ${palette.bg}; --brand-text: ${palette.text}; --radius: 12px; --radius-sm: 8px; --radius-lg: 20px; }\n* { box-sizing: border-box; }\nhtml { scroll-behavior: smooth; }\nbody { background: var(--bg); color: var(--brand-text); font-feature-settings: "rlig" 1, "calt" 1; overflow-x: hidden; }\n::selection { background: ${palette.accent}33; color: ${palette.text}; }\n::-webkit-scrollbar { width: 5px; height: 5px; }\n::-webkit-scrollbar-track { background: transparent; }\n::-webkit-scrollbar-thumb { background: ${palette.accent}44; border-radius: 3px; }\n.gradient-text { background: linear-gradient(135deg, ${palette.accent}, ${palette.accent2}); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }\n.glass { background: rgba(255,255,255,0.04); backdrop-filter: blur(16px); border: 1px solid rgba(255,255,255,0.08); }\n.glow { box-shadow: 0 0 40px ${palette.accent}40; }\n@layer utilities { .animation-delay-100 { animation-delay: 100ms; } .animation-delay-200 { animation-delay: 200ms; } .animation-delay-300 { animation-delay: 300ms; } .animation-delay-500 { animation-delay: 500ms; } }`;

  files['src/app/layout.tsx'] = `import type { Metadata } from "next";\nimport { Inter, Space_Grotesk, JetBrains_Mono } from "next/font/google";\nimport "@/styles/globals.css";\nconst inter = Inter({ subsets: ["latin"], variable: "--font-inter", display: "swap" });\nconst spaceGrotesk = Space_Grotesk({ subsets: ["latin"], variable: "--font-space", display: "swap" });\nconst jetbrainsMono = JetBrains_Mono({ subsets: ["latin"], variable: "--font-mono", display: "swap" });\nexport const metadata: Metadata = { title: { default: "${name}", template: \`%s — ${name}\` }, description: "${template.description}", keywords: ${JSON.stringify(template.tags)}, openGraph: { title: "${name}", description: "${template.description}", type: "website", locale: "en_US" }, twitter: { card: "summary_large_image", title: "${name}", description: "${template.description}" }, robots: { index: true, follow: true }, metadataBase: new URL(process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000") };\nexport default function RootLayout({ children }: { children: React.ReactNode }) { return ( <html lang="en" className={\`\${inter.variable} \${spaceGrotesk.variable} \${jetbrainsMono.variable}\`}> <body className="min-h-screen font-sans antialiased">{children}</body> </html> ); }`;

  files['src/app/page.tsx'] = generatePageTSX(template, name);

  files['src/components/ui/button.tsx'] = `import { cn } from "@/lib/utils";\nimport { forwardRef } from "react";\ninterface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> { variant?: "primary" | "secondary" | "ghost" | "outline"; size?: "sm" | "md" | "lg"; loading?: boolean; }\nconst Button = forwardRef<HTMLButtonElement, ButtonProps>(({ className, variant = "primary", size = "md", loading, children, disabled, ...props }, ref) => { const base = "inline-flex items-center justify-center gap-2 font-semibold rounded-xl transition-all duration-200 outline-none focus-visible:ring-2 focus-visible:ring-offset-2 disabled:opacity-50 disabled:pointer-events-none"; const variants = { primary: "text-white shadow-lg hover:opacity-90 active:scale-95", secondary: "border hover:opacity-80", ghost: "hover:bg-white/5", outline: "border border-current hover:bg-white/5" }; const sizes = { sm: "h-8 px-3 text-sm", md: "h-10 px-5 text-sm", lg: "h-12 px-7 text-base" }; return ( <button ref={ref} disabled={disabled || loading} className={cn(base, variants[variant], sizes[size], className)} style={variant === "primary" ? { background: \`linear-gradient(135deg, \${process.env.NEXT_PUBLIC_ACCENT_COLOR}, \${process.env.NEXT_PUBLIC_ACCENT_COLOR}bb)\`, boxShadow: \`0 4px 24px \${process.env.NEXT_PUBLIC_ACCENT_COLOR}40\` } : undefined} {...props} > {loading && <span className="w-4 h-4 border-2 border-current border-t-transparent rounded-full animate-spin" />} {children} </button> ); });\nButton.displayName = "Button";\nexport { Button };`;

  files['src/lib/utils.ts'] = `import { clsx, type ClassValue } from "clsx";\nimport { twMerge } from "tailwind-merge";\nexport function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }\nexport function formatNumber(n: number): string { if (n >= 1_000_000) return (n / 1_000_000).toFixed(1) + "M"; if (n >= 1_000) return (n / 1_000).toFixed(1) + "K"; return n.toString(); }\nexport function slugify(str: string): string { return str.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, ""); }\nexport function debounce<T extends (...args: unknown[]) => unknown>(fn: T, delay: number) { let timer: ReturnType<typeof setTimeout>; return (...args: Parameters<T>) => { clearTimeout(timer); timer = setTimeout(() => fn(...args), delay); }; }`;

  files['src/hooks/use-scroll-animation.ts'] = `import { useEffect, useRef, useState } from "react";\ninterface UseScrollAnimationOptions { threshold?: number; rootMargin?: string; once?: boolean; }\nexport function useScrollAnimation<T extends HTMLElement>(options: UseScrollAnimationOptions = {}) { const { threshold = 0.1, rootMargin = "0px", once = true } = options; const ref = useRef<T>(null); const [isVisible, setIsVisible] = useState(false);\nuseEffect(() => { const el = ref.current; if (!el) return; const observer = new IntersectionObserver(([entry]) => { if (entry.isIntersecting) { setIsVisible(true); if (once) observer.unobserve(el); } else if (!once) { setIsVisible(false); } }, { threshold, rootMargin }); observer.observe(el); return () => observer.disconnect(); }, [threshold, rootMargin, once]);\nreturn { ref, isVisible }; }`;

  files['src/hooks/use-counter.ts'] = `import { useEffect, useRef, useState } from "react";\nexport function useCounter(end: number, duration = 2000, start = 0) { const [count, setCount] = useState(start); const [active, setActive] = useState(false); const ref = useRef<HTMLElement>(null);\nuseEffect(() => { const el = ref.current; if (!el) return; const obs = new IntersectionObserver(([e]) => { if (e.isIntersecting) { setActive(true); obs.disconnect(); } }, { threshold: 0.5 }); obs.observe(el); return () => obs.disconnect(); }, []);\nuseEffect(() => { if (!active) return; const step = (end - start) / (duration / 16); let current = start; const timer = setInterval(() => { current = Math.min(current + step, end); setCount(Math.round(current)); if (current >= end) clearInterval(timer); }, 16); return () => clearInterval(timer); }, [active, end, start, duration]);\nreturn { count, ref }; }`;

  files['src/types/index.ts'] = `export interface NavItem { label: string; href: string; external?: boolean; }\nexport interface Feature { id: string; icon: string; title: string; description: string; }\nexport interface Testimonial { id: string; name: string; role: string; company: string; avatar: string; content: string; rating: number; }\nexport interface PricingPlan { id: string; name: string; price: number; period: string; description: string; features: string[]; highlighted: boolean; cta: string; }`;

  files['README.md'] = `# ${name}\n\nGenerated with **DevForge** — AI Website Generator\n\n## Stack\n\n- **Framework:** Next.js 14 (App Router)\n- **Language:** TypeScript\n- **Styling:** Tailwind CSS\n- **Animations:** Framer Motion${template.tags.includes('Three.js') ? '\n- **3D:** Three.js + React Three Fiber' : ''}${template.tags.includes('GSAP') ? '\n- **Animation:** GSAP ScrollTrigger' : ''}\n\n## Getting Started\n\n\`\`\`bash\nnpm install\nnpm run dev\n\`\`\`\n\nOpen [http://localhost:3000](http://localhost:3000) in your browser.\n\n## Project Structure\n\n\`\`\`\nsrc/\n├── app/              # Next.js App Router\n│   ├── layout.tsx    # Root layout with fonts & metadata\n│   └── page.tsx      # Home page\n├── components/       # Reusable UI components\n│   ├── ui/           # Primitive UI components\n│   ├── layout/       # Navbar, Footer, etc.\n│   └── sections/     # Page sections\n├── hooks/            # Custom React hooks\n├── lib/              # Utilities and helpers\n├── styles/           # Global CSS\n└── types/            # TypeScript types\n\`\`\`\n\n## Customization\n\nEdit the palette in \`tailwind.config.ts\`:\n- \`accent\`: ${palette.accent}\n- \`accent-secondary\`: ${palette.accent2}\n- \`bg\`: ${palette.bg}\n\n## Deployment\n\n[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new)\n\n\`\`\`bash\nnpm run build    # Build for production\nnpm start        # Start production server\n\`\`\`\n\n---\n*Built with DevForge — devforge.app*`;

  return files;
};

const generatePageTSX = (template: Template, name: string): string => {
  return `"use client";\n\nimport { useEffect, useRef, useState } from "react";\nimport { motion, useScroll, useTransform, AnimatePresence } from "framer-motion";\n${template.tags.includes('Three.js') ? `import { Canvas } from "@react-three/fiber";\nimport { OrbitControls, Sphere, MeshDistortMaterial } from "@react-three/drei";` : ''}\n\nconst SITE = {\n  name: "${name}",\n  tagline: "${template.preview.headline}",\n  sub: "${template.preview.sub}",\n  accent: "${template.palette.accent}",\n};\n\nconst FEATURES = [\n  { icon: "⚡", title: "Lightning Fast", description: "Built for performance from the ground up. Sub-100ms response times guaranteed." },\n  { icon: "🔒", title: "Enterprise Secure", description: "SOC2 Type II certified with end-to-end encryption and role-based access control." },\n  { icon: "🌍", title: "Global Scale", description: "Edge deployment across 50+ regions ensures your users always get the fastest experience." },\n  { icon: "🛠️", title: "Developer First", description: "Comprehensive API, webhooks, and SDKs for every major language and framework." },\n  { icon: "📊", title: "Deep Analytics", description: "Real-time insights with custom dashboards, funnel analysis, and automated reporting." },\n  { icon: "🤝", title: "White-Glove Support", description: "24/7 dedicated support with a 15-minute response SLA for all critical issues." },\n];\n\nconst STATS = [\n  { value: "99.9%", label: "Uptime SLA" },\n  { value: "50ms", label: "Avg. Latency" },\n  { value: "12K+", label: "Active Users" },\n  { value: "4.9★", label: "Rating" },\n];\n\nconst TESTIMONIALS = [\n  { name: "Sarah Chen", role: "CTO at Nexify", avatar: "SC", content: "This has completely transformed how our team operates. The ROI was visible within the first week." },\n  { name: "James Park", role: "Founder at Orbitly", avatar: "JP", content: "Best-in-class product with outstanding support. We've recommended it to every startup in our portfolio." },\n  { name: "Priya Sharma", role: "VP Engineering at Stackr", avatar: "PS", content: "The API is a dream to work with. Clean, well-documented, and exactly what we needed to ship fast." },\n];\n\nfunction Navbar() {\n  const [scrolled, setScrolled] = useState(false);\n  const [open, setOpen] = useState(false);\n  useEffect(() => { const handler = () => setScrolled(window.scrollY > 30); window.addEventListener("scroll", handler, { passive: true }); return () => window.removeEventListener("scroll", handler); }, []);\n  return (\n    <header className={\`fixed top-0 inset-x-0 z-50 transition-all duration-500 \${scrolled ? "py-3 backdrop-blur-2xl border-b" : "py-5"}\`} style={{ background: scrolled ? \`\${SITE.accent}08\` : "transparent", borderColor: scrolled ? \`\${SITE.accent}20\` : "transparent" }}>\n      <nav className="max-w-6xl mx-auto px-6 flex items-center justify-between">\n        <div className="flex items-center gap-3">\n          <div className="w-9 h-9 rounded-xl flex items-center justify-center text-white font-bold text-sm" style={{ background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}bb)\` }}>{SITE.name[0]}</div>\n          <span className="font-display font-bold text-lg">{SITE.name}</span>\n        </div>\n        <div className="hidden md:flex items-center gap-1">\n          {["Features", "Pricing", "Docs", "Blog"].map((item) => (\n            <a key={item} href={\`#\${item.toLowerCase()}\`} className="px-4 py-2 text-sm font-medium opacity-60 hover:opacity-100 rounded-lg hover:bg-white/5 transition-all">{item}</a>\n          ))}\n        </div>\n        <div className="hidden md:flex items-center gap-3">\n          <button className="text-sm font-medium opacity-60 hover:opacity-100 transition-opacity">Sign in</button>\n          <button className="px-5 py-2.5 text-sm font-semibold text-white rounded-xl transition-all hover:scale-105 active:scale-95" style={{ background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}bb)\`, boxShadow: \`0 4px 20px \${SITE.accent}40\` }}>Get Started →</button>\n        </div>\n      </nav>\n    </header>\n  );\n}\n\nfunction HeroSection() {\n  const ref = useRef<HTMLDivElement>(null);\n  const { scrollYProgress } = useScroll({ target: ref, offset: ["start start", "end start"] });\n  const y = useTransform(scrollYProgress, [0, 1], [0, 120]);\n  const opacity = useTransform(scrollYProgress, [0, 0.6], [1, 0]);\n  return (\n    <section ref={ref} className="relative min-h-screen flex items-center justify-center overflow-hidden">\n      <div className="absolute inset-0">\n        <div className="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] rounded-full opacity-20 blur-[120px]" style={{ background: \`radial-gradient(circle, \${SITE.accent}, transparent 70%)\` }} />\n        <div className="absolute top-0 inset-x-0 h-px opacity-20" style={{ background: \`linear-gradient(90deg, transparent, \${SITE.accent}, transparent)\` }} />\n      </div>\n      <motion.div style={{ y, opacity }} className="relative z-10 text-center px-6 max-w-5xl mx-auto pt-24">\n        <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6 }} className="inline-flex items-center gap-2 px-4 py-1.5 rounded-full text-xs font-semibold mb-8 border" style={{ background: \`\${SITE.accent}15\`, borderColor: \`\${SITE.accent}30\`, color: SITE.accent }}>\n          <span className="w-1.5 h-1.5 rounded-full animate-pulse" style={{ background: SITE.accent }} />\n          Now in public beta — Join 12,000+ users\n        </motion.div>\n        <motion.h1 initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.7, delay: 0.1 }} className="font-display text-5xl md:text-7xl font-bold tracking-tight mb-6 leading-[1.08]">\n          {SITE.tagline.split(' ').slice(0, -1).join(' ')}{' '}\n          <span className="inline-block" style={{ background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}bb)\`, WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>{SITE.tagline.split(' ').slice(-1)[0]}</span>\n        </motion.h1>\n        <motion.p initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.7, delay: 0.2 }} className="text-lg md:text-xl opacity-60 max-w-2xl mx-auto mb-10 leading-relaxed">{SITE.sub}</motion.p>\n        <motion.div initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.7, delay: 0.3 }} className="flex flex-col sm:flex-row items-center justify-center gap-4">\n          <button className="px-8 py-4 text-base font-semibold text-white rounded-2xl transition-all hover:scale-105 active:scale-95 flex items-center gap-2" style={{ background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}bb)\`, boxShadow: \`0 8px 40px \${SITE.accent}50\` }}>Start for free <span>→</span></button>\n          <button className="px-8 py-4 text-base font-medium rounded-2xl border border-current opacity-50 hover:opacity-80 transition-opacity flex items-center gap-2">▶ Watch demo</button>\n        </motion.div>\n        <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 1, delay: 0.5 }} className="mt-16 flex items-center justify-center gap-8 flex-wrap">\n          {STATS.map((stat) => (\n            <div key={stat.label} className="text-center">\n              <div className="font-display text-2xl font-bold" style={{ color: SITE.accent }}>{stat.value}</div>\n              <div className="text-xs opacity-40 mt-1">{stat.label}</div>\n            </div>\n          ))}\n        </motion.div>\n      </motion.div>\n    </section>\n  );\n}\n\nfunction FeaturesSection() {\n  return (\n    <section id="features" className="py-32 px-6">\n      <div className="max-w-6xl mx-auto">\n        <div className="text-center mb-20">\n          <div className="text-xs font-bold uppercase tracking-widest opacity-40 mb-4">Features</div>\n          <h2 className="font-display text-4xl md:text-5xl font-bold mb-4">Everything you need to <span style={{ color: SITE.accent }}>ship faster</span></h2>\n          <p className="text-lg opacity-50 max-w-xl mx-auto">A complete toolkit designed for modern teams who care about quality.</p>\n        </div>\n        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">\n          {FEATURES.map((feature, i) => (\n            <motion.div key={feature.title} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.5, delay: i * 0.07 }} whileHover={{ y: -4 }} className="p-6 rounded-2xl border border-white/5 hover:border-white/10 transition-all" style={{ background: "rgba(255,255,255,0.025)" }}>\n              <div className="w-12 h-12 rounded-xl flex items-center justify-center text-2xl mb-4" style={{ background: \`\${SITE.accent}15\` }}>{feature.icon}</div>\n              <h3 className="font-display font-semibold text-lg mb-2">{feature.title}</h3>\n              <p className="text-sm opacity-50 leading-relaxed">{feature.description}</p>\n            </motion.div>\n          ))}\n        </div>\n      </div>\n    </section>\n  );\n}\n\nfunction TestimonialsSection() {\n  return (\n    <section className="py-32 px-6">\n      <div className="max-w-6xl mx-auto">\n        <div className="text-center mb-20">\n          <div className="text-xs font-bold uppercase tracking-widest opacity-40 mb-4">Testimonials</div>\n          <h2 className="font-display text-4xl font-bold">Loved by <span style={{ color: SITE.accent }}>thousands</span></h2>\n        </div>\n        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">\n          {TESTIMONIALS.map((t, i) => (\n            <motion.div key={t.name} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.1 }} className="p-6 rounded-2xl border" style={{ borderColor: \`\${SITE.accent}20\`, background: \`\${SITE.accent}08\` }}>\n              <div className="flex items-center gap-1 mb-4">\n                {[1,2,3,4,5].map(s => <span key={s} style={{ color: SITE.accent }}>★</span>)}\n              </div>\n              <p className="text-sm opacity-70 leading-relaxed mb-5">\"{t.content}\"</p>\n              <div className="flex items-center gap-3">\n                <div className="w-9 h-9 rounded-full flex items-center justify-center text-xs font-bold text-white" style={{ background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}80)\` }}>{t.avatar}</div>\n                <div>\n                  <div className="text-sm font-semibold">{t.name}</div>\n                  <div className="text-xs opacity-40">{t.role}</div>\n                </div>\n              </div>\n            </motion.div>\n          ))}\n        </div>\n      </div>\n    </section>\n  );\n}\n\nfunction PricingSection() {\n  const [annual, setAnnual] = useState(false);\n  const plans = [\n    { name: "Starter", price: annual ? 19 : 29, desc: "Perfect for individuals", features: ["5 projects", "Basic analytics", "API access", "Email support"], popular: false },\n    { name: "Pro", price: annual ? 59 : 79, desc: "For growing teams", features: ["Unlimited projects", "Advanced analytics", "Priority support", "Custom domains", "Team collaboration"], popular: true },\n    { name: "Enterprise", price: annual ? 199 : 249, desc: "For large organizations", features: ["Everything in Pro", "SSO & SAML", "SLA guarantee", "Dedicated manager", "Custom contracts"], popular: false },\n  ];\n  return (\n    <section id="pricing" className="py-32 px-6">\n      <div className="max-w-5xl mx-auto">\n        <div className="text-center mb-16">\n          <div className="text-xs font-bold uppercase tracking-widest opacity-40 mb-4">Pricing</div>\n          <h2 className="font-display text-4xl font-bold mb-6">Simple, <span style={{ color: SITE.accent }}>transparent</span> pricing</h2>\n          <div className="inline-flex items-center gap-3 p-1.5 rounded-xl border border-white/10">\n            {["Monthly", "Annual"].map((p) => (\n              <button key={p} onClick={() => setAnnual(p === "Annual")} className={\`px-5 py-2 text-sm font-medium rounded-lg transition-all \${(p === "Annual") === annual ? "text-white" : "opacity-50"}\`} style={(p === "Annual") === annual ? { background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}bb)\` } : {}}>\n                {p} {p === "Annual" && <span className="opacity-60 ml-1">−20%</span>}\n              </button>\n            ))}\n          </div>\n        </div>\n        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">\n          {plans.map((plan, i) => (\n            <motion.div key={plan.name} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.1 }} className={\`p-7 rounded-2xl border relative overflow-hidden \${plan.popular ? 'ring-2' : ''}\`} style={{ background: plan.popular ? \`\${SITE.accent}12\` : "rgba(255,255,255,0.025)", borderColor: plan.popular ? SITE.accent : "rgba(255,255,255,0.08)" }}>\n              {plan.popular && <div className="absolute top-4 right-4 text-[10px] font-bold px-2.5 py-1 rounded-full text-white" style={{ background: SITE.accent }}>POPULAR</div>}\n              <div className="text-sm font-semibold opacity-50 mb-2">{plan.name}</div>\n              <div className="font-display text-4xl font-bold mb-1">${'\u0024'}{plan.price}<span className="text-lg opacity-40 font-normal">/mo</span></div>\n              <div className="text-sm opacity-40 mb-6">{plan.desc}</div>\n              <button className={\`w-full py-3 rounded-xl text-sm font-semibold transition-all hover:scale-105 active:scale-95 mb-6 \${plan.popular ? 'text-white' : 'border border-current opacity-60 hover:opacity-100'}\`} style={plan.popular ? { background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}bb)\`, boxShadow: \`0 4px 20px \${SITE.accent}40\` } : {}}>\n                {plan.popular ? "Start free trial →" : "Get started"}\n              </button>\n              <ul className="space-y-3">\n                {plan.features.map((f) => (\n                  <li key={f} className="flex items-center gap-2.5 text-sm">\n                    <span className="text-base" style={{ color: SITE.accent }}>✓</span>\n                    <span className="opacity-70">{f}</span>\n                  </li>\n                ))}\n              </ul>\n            </motion.div>\n          ))}\n        </div>\n      </div>\n    </section>\n  );\n}\n\nfunction CTASection() {\n  return (\n    <section className="py-32 px-6">\n      <div className="max-w-3xl mx-auto text-center">\n        <div className="p-16 rounded-3xl border relative overflow-hidden" style={{ background: \`radial-gradient(circle at 50% 100%, \${SITE.accent}20, transparent 70%)\`, borderColor: \`\${SITE.accent}25\` }}>\n          <div className="absolute inset-0 blur-3xl opacity-30" style={{ background: \`radial-gradient(circle at 50% 120%, \${SITE.accent}, transparent 60%)\` }} />\n          <div className="relative">\n            <h2 className="font-display text-4xl md:text-5xl font-bold mb-4">Ready to get started?</h2>\n            <p className="text-lg opacity-50 mb-10">Join 12,000+ teams already using {SITE.name} to build faster.</p>\n            <div className="flex flex-col sm:flex-row items-center justify-center gap-4">\n              <button className="px-8 py-4 text-base font-semibold text-white rounded-2xl transition-all hover:scale-105 flex items-center gap-2" style={{ background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}bb)\`, boxShadow: \`0 8px 40px \${SITE.accent}50\` }}>\n                Start free — no credit card required\n              </button>\n            </div>\n            <p className="text-xs opacity-30 mt-6">No credit card required · 14-day free trial · Cancel anytime</p>\n          </div>\n        </div>\n      </div>\n    </section>\n  );\n}\n\nfunction Footer() {\n  return (\n    <footer className="py-12 px-6 border-t border-white/5">\n      <div className="max-w-6xl mx-auto flex flex-col md:flex-row items-center justify-between gap-6">\n        <div className="flex items-center gap-2 font-display font-bold">\n          <div className="w-7 h-7 rounded-lg flex items-center justify-center text-white text-sm font-bold" style={{ background: \`linear-gradient(135deg, \${SITE.accent}, \${SITE.accent}bb)\` }}>{SITE.name[0]}</div>\n          {SITE.name}\n        </div>\n        <div className="flex items-center gap-6">\n          {["Privacy", "Terms", "Docs", "Blog", "Status"].map((l) => <a key={l} href="#" className="text-sm opacity-40 hover:opacity-80 transition-opacity">{l}</a>)}\n        </div>\n        <div className="text-sm opacity-30">© 2026 {SITE.name}. All rights reserved.</div>\n      </div>\n    </footer>\n  );\n}\n\nexport default function HomePage() {\n  return (\n    <main>\n      <Navbar />\n      <HeroSection />\n      <FeaturesSection />\n      <TestimonialsSection />\n      <PricingSection />\n      <CTASection />\n      <Footer />\n    </main>\n  );\n}`;
};
